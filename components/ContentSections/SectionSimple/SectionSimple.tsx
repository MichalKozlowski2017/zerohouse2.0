import React from 'react';
import { Block } from '@typings/block';

const SectionSimple = (block: Block) => {
  console.log(block);
  return <div>SectionSimple</div>;
};

export default SectionSimple;
